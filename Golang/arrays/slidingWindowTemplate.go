package arrays


/*

for right := 0; right < len(nums); right++ {
	// always grow the window first
	windowSum += nums[right]

	// Now we might need to shrink multiple times
	while windowSum > target {
		windowSum -= nums[left]
		left++
	}
}
	*/