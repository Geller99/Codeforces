### Problem: Three Sum
Pattern: Sort + Two Pointers

`Core Insight`:
Fix one number and reduce the problem to Two Sum using pointers.

`What I missed initially`:
I only checked consecutive elements and didn’t recognize the need to reduce 3Sum → 2Sum.

`Interview warning`:
Without sorting, duplicate handling becomes unmanageable.