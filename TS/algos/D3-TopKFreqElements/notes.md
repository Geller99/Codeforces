

Pattern - arrays and hashing

Questions/Test/Edge Cases

- Do negative values count as duplicates? [1, -1, 2, -2, 3, -3]
- What do we return if there are no duplicates in the array but multiple values? [1,2,3]
- What do we return if there is an equal number of duplicates in the array? [1,1,2,2,3,3], which ones are the k most frequent?
- What do we return if the array is empty? return 0
- What do we return if k > array length? return 0
